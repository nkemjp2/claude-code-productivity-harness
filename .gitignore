node_modules/
*.log

# Generated by the hook generator; never hand-edited (M2).
# Tracked deliberately from Phase 2 onward so CI can diff it against a
# regeneration — until then there is nothing to ignore or keep.

# Runtime state, not source. One file per process, named by session and pid
# (M26), and rotated by the retention window in policy.yaml.
.harness/events/
.harness/tasks/*/evidence/
